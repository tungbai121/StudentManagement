<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studentmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SearchStudentActivity"
            android:label="Tìm Kiếm Sinh Viên" />
        <activity
            android:name=".activities.SearchClassActivity"
            android:label="Tìm Kiếm Lớp" />
        <activity
            android:name=".activities.EditStudentActivity"
            android:label="Quản Lý Sinh Viên" />
        <activity
            android:name=".activities.StudentActivity"
            android:label="Quản Lý Sinh Viên" />
        <activity
            android:name=".activities.EditClassActivity"
            android:label="Quản Lý Lớp" />
        <activity
            android:name=".activities.ClassActivity"
            android:label="Quản Lý Lớp" />
        <activity
            android:name=".activities.EditFacultyActivity"
            android:label="Quản Lý Khoa" />
        <activity
            android:name=".activities.FacultyActivity"
            android:label="Quản Lý Khoa" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>